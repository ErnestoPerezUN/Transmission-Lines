# Núcleo: parámetros, FDTD, campo eléctrico, capacidad de corriente y tests.
# Versiones probadas el 2026-09-07 con Python 3.12 (ver CLAUDE.md).
numpy>=2.0
scipy>=1.15
matplotlib>=3.10
pandas>=2.2
seaborn>=0.13
pillow>=10          # GIF animado de examples/voltage_source_on_line.py --save
pytest>=8
nbstripout>=0.8     # notebooks sin salidas en git (ver .gitattributes)
